<view class="tab-bar">
  <view
    wx:for="{{list}}"
    wx:key="pagePath"
    class="tab-item {{selected === index ? 'active' : ''}}"
    data-index="{{index}}"
    bindtap="switchTab"
  >
    <text class="tab-icon">{{selected === index ? item.iconSelected : item.icon}}</text>
    <text class="tab-text">{{item.text}}</text>
  </view>
</view>
